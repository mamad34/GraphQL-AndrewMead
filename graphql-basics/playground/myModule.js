const message = "dick to in zendegi";

const name = "mamad";

const location = "iran";

const getGreeting = (name) => {
  return `welcome to the course ${name}`;
};

export { message, name, getGreeting, location as default };
